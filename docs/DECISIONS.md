# Implementation Decisions

- Admission fees are deducted inside `Store.submit_claim`. This satisfies the requirement that the fee is charged at submission regardless of admission outcome and keeps rejected claims accountable even before the coordinator polls.
- Challenge graph changes are append-only citation overrides in `claim_cite_overrides`, not mutations to admitted `claims`. Settlement snapshots apply the latest override so the challenge graph is post-resolution while the base claim ledger stays immutable.
- Payouts are transferred from pseudo escrow accounts such as `ESCROW:<task_id>`. The escrow debit from the sponsor is recorded at task creation, but pseudo accounts are not identities because the spec only requires identity balances.
- The synthetic fact task uses independent fact subtasks plus an answer subtask depending on all facts. This exercises leases and the dependency DAG while allowing multiple local workers to race on discovery.
- The optional LLM path is implemented behind the verifier interface with an OpenAI-compatible HTTP hook. Tests skip it when `VSCP_LLM` is unset and use a crafted completion function when enabled.
- `FAIL` claims are verified negative results: `MockVerifier` admits one only if the body is a decoy per the answer key *and* appears in the evidence spans. Bodies that merely claim failure are rejected. Scripted workers publish a `FAIL` (with an evidence ref, no subtask) for every decoy hit and read others' FAILs through the metered `fetch` to prune their candidate sets — this is what makes the steering pool (γ) flow end-to-end instead of burning.
- Cited admitted claims count as support spans at admission. Consequently `ANSWER` claims carry provenance via `cites` only — no re-attached upstream evidence, and a compact O(1) body verified by coverage (every key fact present in the spans) instead of verbatim listing. The spec's verbatim-listing rule (§3) conflicts with the 600-char body cap (§6.1) for K ≥ 8; coverage via cites resolves the conflict and removes the 8-ref cap on K entirely.
- Cites must target claims of the same task (`cross_task_cite` rejection); the settlement snapshot is per-task, so cross-task edges would dangle.
- `resolve_ref_span` returns the *minimal* valid head→tail span (deterministic tie-break on earliest head). Returning the first match let an author pick a common head and a distant tail to capture text they never localized.
- `store.fetch` serves ADMITTED claim bodies only; PENDING/REJECTED bodies would leak pre-admission information. Evidence documents are always servable.
- Pseudo accounts (`ESCROW:*`, `BOND:*`, `FEE_POOL`) are tracked in a `pseudo_accounts` table with a non-negative CHECK; admission fees are routed into `FEE_POOL` so refunds draw against a real balance. A global conservation test asserts identities + pseudo accounts + burns equal total minted.
- Challenges resolve atomically: verdict is computed read-only, then bond, cite override, payouts, and the challenge row commit in a single transaction. An upheld slash is capped at the target author's balance rather than wedging resolution. Bond asymmetry is deliberate: a failed under-citation bond burns (the check is deterministic; frivolous filings are spam), while a failed materiality bond pays the target author (the challenger consumed verifier work against a real citation).
- The scripted hoarder is a *racing* hoarder: it free-rides on published facts via `fetch` and submits an unleased `ANSWER` the moment all facts are admitted, paying only the admission fee. It loses the race to the honest finisher and forfeits the fee, which is the incentive claim the demo actually needs to exercise.

## Phase 2

- `settle(snapshot, version=None)`: an explicit `version` argument takes precedence over the snapshot's `settlement_version` key (caller intent wins, e.g. for v1-vs-v2 comparisons on the same snapshot); absent both, the default is `SETTLEMENT_VERSION = 2`. The coordinator stamps `tasks.settlement_version` at close and `store.snapshot` carries it.
- Steering v2 ties endorsement to *informed work*: a fetch only counts if the reader later (epoch-wise: fetch epoch <= productive claim's `epoch_submitted`) authored a positively-flowing claim whose docs overlap the FAIL's docs, and each productive reader's endorsement is normalized to `1_000_000` units. In the seed-42 demo the finisher is effectively the only eligible reader (per-doc FACT authors author the FAILs on their own doc, which never count), so gamma flows through the finisher's normalized endowment - still fully paid, no longer collusion-priced.
- `delm_market_sim` was not available in this environment, so `tools/sim/sim.py` is an *independently written* float-arithmetic reference implementation of the same mechanism (recursive flow propagation, per-author accumulation) rather than a vendored copy. The equivalence harness's purpose - two independent implementations cross-checking each other - is preserved; if the original sim surfaces, drop it in and rerun the same scenarios.
- Scenario claims without an explicit `"epoch"` key use their list index as `epoch_submitted` (submission order doubles as the epoch).
- Challenge slashes are capped at the target author's balance inside the single resolution transaction (Phase 1 decision, restated here because §2 of the Phase 2 spec depends on resolved challenge graphs).
- ANSWER-body discipline: admission requires `^ANSWER: ` and the existing 600-char cap. Semantic verification of answer bodies against cited claims is performed by the hardened LLM verifier (§5); the mock path performs the coverage check only (every key fact present in the support spans) and deliberately nothing more.
- The corpus documents are original texts written for this repo (effectively CC0) of ~1.0-1.3 kB each, slightly below the spec's 2-8 kB suggestion, keeping fake-LLM CI prompts compact; the loader imposes no size constraint, so larger public-domain texts drop in unchanged.
- `legion eval` selects its backend automatically: the real OpenAI-compatible endpoint iff `VSCP_LLM=1`, otherwise a deterministic fake `complete` stub that answers from the fixture's gold facts. The stub is also what CI runs.
- The LLM worker's local verbatim check runs before `submit_claim`, so hallucinated sentences cost zero fees; after 3 failed extraction attempts (initial + two resubmissions) the subtask is abandoned for that worker. The per-worker call budget (`VSCP_MAX_LLM_CALLS_PER_WORKER`, default 30) is shared across extraction and synthesis calls; verifier calls are counted separately by the eval harness.
- A realdoc `FAIL` is a *verified irrelevance statement* (a sentence asserted irrelevant despite appearing relevant), exposed via `LLMWorker.submit_irrelevance_fail` and admitted only if the verifier supports it; model output that merely fails the verbatim check is a worker error and is never published.

## Phase 2 review fixes

- `cli.py` gained the missing `if __name__ == "__main__"` guard: `python -m legion.cli` used to import and exit 0 silently. Module invocation is now pinned by `tests/test_cli_entrypoint.py`, including an actual demo run, because a silently-succeeding no-op command is the worst failure mode.
- The long-document fixture `deep_archive` (three ~12 kB documents under `corpus/long/`) is generated by the committed deterministic script `tools/make_long_fixture.py`; the generated text is committed too, so eval needs no generation step. Hand-authoring 36 kB of prose was not a good use of anyone's time; the gold-fact sentences are hand-written and embedded verbatim mid-document. This puts one fixture into the multi-document regime the architecture targets, where the single-pass baseline's context cost grows with corpus size.
- The steering-concentration observation was promoted to a measured metric: `settlement.eligible_steering_readers(snapshot)` (readers with a non-empty v2 eligibility set) is reported per task as `steering_readers` in `report.json` and the eval table. On single-doc-per-subtask decompositions it collapses to the finisher; fan-out on overlapping-evidence tasks is the Phase 3 signal that search paths crossed.
- §4A of spec v2.1 (sim world model, strategy zoo, statistical goldens, Shapley estimator) was **not** implemented in Phase 2: the repo's `goal.md` was spec v2.0 and the v2.1 text was not available. Implemented in Phase 3 from the Phase 3 spec's restatement (see below).

## Phase 3

- **§4A sim design gaps** (the v2.1 §4A text was still unavailable; the Phase 3 spec's restatement names the seven strategies and three orderings, the rest is filled with the simplest consistent choices): one oracle probe per agent per epoch; submissions become visible at epoch end (the sim's admission tick, which is what makes same-epoch duplicate publication possible); all agents that can see a complete fact set race the answer in the same epoch, all pay the fee, the first in queue order wins; constants `ADMISSION_FEE=0.005`, `PROBE_COST=0.01`, `VERIFIER_LEAK=0.4` (probability a plausible decoy-as-FACT slips the semantic check - the poisoner's channel); the Shapley settlement is a 120-permutation sampled estimator over *apparent* document coverage, deliberately the naive exploitable attribution the flat design replaces; RING_SYBIL mints post-hoc trivially-true FAILs on already-solved docs that only its partner reads; fee refunds are not modeled (net = payouts - fees - probe costs).
- **§4A.6 measured golden values** at the pinned seeds (bands in `tests/test_sim_goldens.py` may widen only with these updated): honest 0.2005 vs hoarder -0.005; poisoner flat -0.017 / Shapley +0.0389; sybil steering v1 0.2067 / v2 0.0; spammer -0.0158 (both settlements); all-hybrid welfare 12.19 vs all-honest 16.86 (ratio 0.723 ≤ 0.75); duplicates 0.0 vs originals 2.925 over 22 duplicate-bearing episodes; keep-fraction padded income exactly equal (0.1125); seed determinism exact.
- The long fixture was regenerated at six ~22 kB documents (spec's 20-50 kB band, 4-8 docs) and renamed `long_deep_archive.json`; same committed deterministic generator.
- LLM workers gained a read-then-extract epoch gap, FAIL absorption through the metered fetch, and **idle triage** (an idle worker publishes one verified irrelevance FAIL on a document someone else is mining). This is what makes steering-v2 eligibility fan out on multi-doc tasks (`distinct_eligible_steering_readers` ≥ 2 with 8 workers on the long fixture) instead of collapsing to the finisher.
- Eval metrics: `verifier_calls` are counted on a separate counter from worker calls; `prompt_chars`/`char_ratio` were added beyond spec because per-call `cost_ratio` is structurally unwinnable for the protocol (baseline = 1 call by construction) and context volume is the honest cost measure - see `docs/REGIME_FINDINGS.md`.
- Multi-process discipline: the coordinator is the sole writer for admission/epoch/close/settlement; workers only lease/fetch/submit; lease acquisition is `BEGIN IMMEDIATE` + `UPDATE ... WHERE status='PENDING'` with the winner decided by rowcount; `PRAGMA busy_timeout=5000` on every connection; writers retry on SQLITE_BUSY up to 5 times with linear backoff. Workers swallow "task ledger is frozen" races (close happened between their status check and submit) by standing down.
- Genesis grants are now mirrored as `MINT` transfers (from NULL) so the light client can re-derive every balance from `transfers` alone; `create_identity` emits one only when the row is newly inserted with a positive balance. `MINT` is a transfer reason, not a claim kind - the no-new-claim-kinds invariant stands.
- The light client (`tools/lightclient.py`, `legion audit`) re-derives balances, settlements, and deterministic admission checks from a read-only connection. Trust boundary stated explicitly: semantic verifier verdicts and the lease state observed at admission are not re-derivable offline. It reuses only pure engine functions (`settle`, `resolve_ref_span`, `crypto`), per the spec's allowance; the `audit` CLI inserts the repo root on sys.path to reach `tools/` (works for the editable install this repo uses).
- The tamper test drops the append-only trigger before rewriting a payout: triggers are integrity rails, not security boundaries, and the audit is precisely what catches file-level rewrites after the fact.

## Phase 3.1

- **The fake stub is answer-key-free.** `heuristic_fake_complete` extracts by deterministic keyword-overlap between the question and document sentences; it never sees `gold_facts`. Fixture questions were rewritten so each gold sentence is the top-overlap sentence in its own document (a vague question honestly misses - that is the trap fixture `tests/fixtures/miss_trap.json`, which both the protocol and the iterative baseline fail, proving the grader is real). Gold facts are referenced only in the grader (`_grade`).
- **Prompt-leak invariant interpretation**: gold facts textually appear inside their source documents, so "no gold fact in any prompt" is literally unsatisfiable for a system that must read the documents. The enforced data-flow property is: a gold fact may appear in a prompt only (a) inside its full source document or (b) after the model itself produced it in a prior completion; any other appearance is a harness leak and fails the test.
- **Baseline definitions**: `baseline_onecall` is the infinite-context oracle reference (kept, relabelled, marked `infeasible` - never truncated - when the estimated prompt exceeds `VSCP_CONTEXT_WINDOW_TOKENS`, default 128000); `baseline_iterative` is the fair comparator (one extraction per document, one synthesis, one optional re-query, hard cap n_docs+2 calls, same documents the workers fetch, not hobbled).
- **Token approximation**: tokens are estimated as ceil(chars/4) over prompt and completion; provider `usage` numbers supersede the estimate when the real endpoint returns them (`token_source: provider|estimated` recorded per run). `token_ratio` vs the iterative baseline is the headline; the per-call ratio is kept as a clearly-labelled legacy secondary because per-call pricing structurally penalizes any decomposed system. `VSCP_COST_PER_1K_TOKENS` (default 0.15) drives `est_cost_*`.
- **XL fixture**: `xl_town_archive` (8 docs x ~80 kB ~= 160k estimated tokens) deliberately exceeds the default window so the one-call-infeasible branch runs in CI on the fake path; generated by the same committed script as the long fixture. Sweep doc classes are now short/long/xl by filename prefix.
- **OpenRouter client** (`legion/llm_client.py`): default endpoint `https://openrouter.ai/api/v1/chat/completions`, default model `openai/gpt-4o-mini`, `VSCP_VERIFIER_MODEL` may override for the verifier; key resolution OPENROUTER_API_KEY > VSCP_LLM_API_KEY > OPENAI_API_KEY; optional HTTP-Referer/X-Title from env; temperature 0, 30 s timeout, exactly one transport retry, never a retry on contract failure; the key never appears in any error or log. `LLMVerifier` delegates to it; injection defenses unchanged.
- **Budget rails**: `VSCP_MAX_TOTAL_LLM_CALLS` (default 500) is a per-run hard cap shared across protocol, baselines, and verifier; exceeding it stops call issuance, marks `budget_capped: true`, and still emits a partial report. `legion eval --dry-run` prints backend, model, endpoint, estimated max calls, and estimated max cost (assuming 2000 tokens/call, stated in the printout) and exits without calling.
- Phase 3's `REGIME_FINDINGS.md` numbers were measured against the false baseline and are superseded; the rewritten document says so prominently.
